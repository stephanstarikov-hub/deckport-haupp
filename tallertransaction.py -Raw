warning: in the working copy of 'main.py', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'package.json', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'vpn/service.py', CRLF will be replaced by LF the next time Git touches it
[1mdiff --git a/main.py b/main.py[m
[1mindex 8e3aef9..58ad8ef 100644[m
[1m--- a/main.py[m
[1m+++ b/main.py[m
[36m@@ -8,38 +8,22 @@[m [mimport decky[m
 ROOT = Path(__file__).resolve().parent[m
 sys.path.insert(0, str(ROOT / "py_modules"))[m
 sys.path.insert(0, str(ROOT))[m
[31m-from vpn.service import Service[m
[32m+[m[32mfrom vpn.ipc import Client[m
 from vpn.errors import VPNError[m
 [m
 [m
 class Plugin:[m
     async def _main(self):[m
[31m-        os.umask(0o077)[m
[31m-        self.ready = asyncio.Event()[m
[31m-        self.service = None[m
[31m-        try:[m
[31m-            self.service = Service(ROOT, Path(decky.DECKY_PLUGIN_SETTINGS_DIR), Path(decky.DECKY_PLUGIN_RUNTIME_DIR), Path(decky.DECKY_PLUGIN_LOG_DIR), decky.emit)[m
[31m-            await self.service.initialize()[m
[31m-        except Exception:[m
[31m-            decky.logger.error("Decky VPN initialization failed; check storage permissions")[m
[31m-        finally:[m
[31m-            self.ready.set()[m
[32m+[m[32m        self.client = Client()[m
 [m
     async def _rpc(self, method, *args):[m
         try:[m
[31m-            if not hasattr(self, "ready"):[m
[31m-                raise VPNError("VPN backend is starting; try again")[m
[31m-            await asyncio.wait_for(self.ready.wait(), 30)[m
[31m-            if self.service is None:[m
[31m-                raise VPNError("VPN backend could not initialize its private storage")[m
[31m-            result = getattr(self.service, method)(*args)[m
[31m-            if asyncio.iscoroutine(result):[m
[31m-                result = await result[m
[31m-            return {"ok": True, "data": result}[m
[31m-        except VPNError as e:[m
[31m-            return {"ok": False, "error": str(e)}[m
[32m+[m[32m            if not hasattr(self, "client"):[m
[32m+[m[32m                self.client = Client()[m
[32m+[m[32m            return {"ok": True, "data": await self.client.call(method, *args)}[m
[32m+[m[32m        except VPNError as error:[m
[32m+[m[32m            return {"ok": False, "error": str(error)}[m
         except Exception:[m
[31m-            # Never return repr(exception): urllib/core errors may contain secrets.[m
             return {"ok": False, "error": "Operation failed; view safe diagnostic information"}[m
 [m
     async def get_status(self):[m
[36m@@ -90,10 +74,13 @@[m [mclass Plugin:[m
     async def get_diagnostic_info(self):[m
         return await self._rpc("diagnostic")[m
 [m
[32m+[m[32m    async def set_favorite(self, server_id, enabled):[m
[32m+[m[32m        return await self._rpc("favorite", server_id, enabled)[m
[32m+[m
     async def _unload(self):[m
[31m-        if getattr(self, "service", None):[m
[31m-            await self.service.close()[m
[32m+[m[32m        # A client going away must never disconnect the system VPN.[m
[32m+[m[32m        pass[m
 [m
     async def _uninstall(self):[m
[31m-        if getattr(self, "service", None):[m
[31m-            await self.service.disconnect()[m
[32m+[m[32m        # Removing the Decky client also leaves the Desktop-owned service alone.[m
[32m+[m[32m        pass[m
[1mdiff --git a/package.json b/package.json[m
[1mindex 716e08d..2b1c48c 100644[m
[1m--- a/package.json[m
[1m+++ b/package.json[m
[36m@@ -1,6 +1,6 @@[m
 {[m
   "name": "decky-vpn",[m
[31m-  "version": "0.1.4",[m
[32m+[m[32m  "version": "0.2.0",[m
   "description": "DeckPort VPN — subscription VPN inside Steam Deck Gaming Mode",[m
   "type": "module",[m
   "license": "GPL-3.0-or-later",[m
[1mdiff --git a/vpn/service.py b/vpn/service.py[m
[1mindex 8a77d8c..9c9b9f7 100644[m
[1m--- a/vpn/service.py[m
[1m+++ b/vpn/service.py[m
[36m@@ -4,6 +4,8 @@[m [mimport json[m
 import logging[m
 import ipaddress[m
 import socket[m
[32m+[m[32mimport os[m
[32m+[m[32mimport hashlib[m
 from logging.handlers import RotatingFileHandler[m
 from pathlib import Path[m
 import time[m
[36m@@ -16,6 +18,8 @@[m [mfrom .storage import Store, private_dir, atomic_json[m
 from .subscription.fetch import download, validate_url[m
 from .subscription.model import public_node[m
 from .subscription.parser import parse[m
[32m+[m[32mfrom .safe_fs import read_regular[m
[32m+[m[32mfrom .version import VERSION, PROTOCOL_VERSION[m
 [m
 [m
 class Service:[m
[36m@@ -33,6 +37,12 @@[m [mclass Service:[m
         self.task = None[m
         self.monitor = None[m
         self.closing = False[m
[32m+[m[32m        self.maintenance = False[m
[32m+[m[32m        self.legacy_import_dir = None[m
[32m+[m[32m        self.retry_at = 0[m
[32m+[m[32m        self.retry_delay = 2[m
[32m+[m[32m        self.network_signature = None[m
[32m+[m[32m        self.last_tick = self.boottime()[m
         self.logger = logging.getLogger("decky-vpn-safe")[m
         self.logger.setLevel(logging.INFO)[m
         self.logger.propagate = False[m
[36m@@ -46,6 +56,8 @@[m [mclass Service:[m
         except VPNError as e:[m
             await self.set_state("ERROR", error=str(e))[m
         self.monitor = asyncio.create_task(self.watch())[m
[32m+[m[32m        if self.store.data["desired_connection"] and self.store.data["selected"]:[m
[32m+[m[32m            await self.set_state("RECONNECTING", error=None)[m
 [m
     async def set_state(self, state, **kwargs):[m
         self.state.update(state=state, **kwargs)[m
[36m@@ -58,7 +70,7 @@[m [mclass Service:[m
 [m
     def status(self):[m
         result = copy.deepcopy(self.state)[m
[31m-        result.update(selected_server=None, selected_subscription=None)[m
[32m+[m[32m        result.update(selected_server=None, selected_subscription=None, version=VERSION, maintenance=self.maintenance)[m
         for subscription in self.store.data["subscriptions"]:[m
             for n in subscription["nodes"]:[m
                 if n["id"] == self.state["selected"]:[m
[36m@@ -99,12 +111,19 @@[m [mclass Service:[m
     def import_files(self):[m
         files = [][m
 [m
[31m-        for path in self.import_dir.iterdir():[m
[32m+[m[32m        paths = list(self.import_dir.iterdir())[m
[32m+[m[32m        if self.legacy_import_dir and self.legacy_import_dir.is_dir() and not self.legacy_import_dir.is_symlink():[m
[32m+[m[32m            paths += list(self.legacy_import_dir.iterdir())[:100][m
[32m+[m[32m        seen = set()[m
[32m+[m[32m        for path in paths:[m
             try:[m
                 if path.is_symlink() or not path.is_file():[m
                     continue[m
 [m
                 self.validate_import_filename(path.name)[m
[32m+[m[32m                if path.name in seen:[m
[32m+[m[32m                    continue[m
[32m+[m[32m                seen.add(path.name)[m
 [m
                 stat = path.stat()[m
                 if stat.st_size > 8 * 1024 * 1024:[m
[36m@@ -124,6 +143,8 @@[m [mclass Service:[m
     def read_import_file(self, filename):[m
         filename = self.validate_import_filename(filename)[m
         path = self.import_dir / filename[m
[32m+[m[32m        if not path.exists() and self.legacy_import_dir:[m
[32m+[m[32m            path = self.legacy_import_dir / filename[m
 [m
         if not path.exists() or path.is_symlink() or not path.is_file():[m
             raise VPNError("Import file not found")[m
[36m@@ -132,9 +153,26 @@[m [mclass Service:[m
             raise VPNError("Subscription file is too large")[m
 [m
         try:[m
[31m-            return path.read_text(encoding="utf-8-sig")[m
[32m+[m[32m            return read_regular(path, 4 * 1024 * 1024).decode("utf-8-sig")[m
         except UnicodeDecodeError:[m
             raise VPNError("Subscription file must be UTF-8 text") from None[m
[32m+[m[32m        except (OSError, ValueError):[m
[32m+[m[32m            raise VPNError("Subscription file could not be read safely") from None[m
[32m+[m
[32m+[m[32m    async def import_content(self, content, name):[m
[32m+[m[32m        if len(content.encode("utf-8")) > 4 * 1024 * 1024:[m
[32m+[m[32m            raise VPNError("Subscription file is too large")[m
[32m+[m[32m        filename = uuid.uuid4().hex + ".txt"[m
[32m+[m[32m        target = self.import_dir / filename[m
[32m+[m[32m        # Unique daemon-owned path. No client-provided filesystem path is used.[m
[32m+[m[32m        with target.open("x", encoding="utf-8") as stream:[m
[32m+[m[32m            stream.write(content)[m
[32m+[m[32m        target.chmod(0o600)[m
[32m+[m[32m        try:[m
[32m+[m[32m            return await self.import_subscription(filename, name)[m
[32m+[m[32m        except BaseException:[m
[32m+[m[32m            target.unlink(missing_ok=True)[m
[32m+[m[32m            raise[m
 [m
     async def import_subscription(self, filename, name=None, identifier=None):[m
         filename = self.validate_import_filename(filename)[m
[36m@@ -267,7 +305,38 @@[m [mclass Service:[m
         raise VPNError("Server not found; refresh the server list")[m
 [m
     def servers(self, subscription_id):[m
[31m-        return [public_node(n) for n in self.find_subscription(subscription_id)["nodes"]][m
[32m+[m[32m        return [dict(public_node(n), favorite=n["id"] in self.store.data["favorites"]) for n in self.find_subscription(subscription_id)["nodes"]][m
[32m+[m
[32m+[m[32m    async def favorite(self, identifier, enabled):[m
[32m+[m[32m        self.find_node(identifier)[m
[32m+[m[32m        favorites = set(self.store.data["favorites"])[m
[32m+[m[32m        if enabled:[m
[32m+[m[32m            favorites.add(identifier)[m
[32m+[m[32m        else:[m
[32m+[m[32m            favorites.discard(identifier)[m
[32m+[m[32m        self.store.data["favorites"] = sorted(favorites)[m
[32m+[m[32m        self.store.save()[m
[32m+[m[32m        return enabled[m
[32m+[m
[32m+[m[32m    def preferences(self):[m
[32m+[m[32m        return copy.deepcopy(self.store.data["preferences"])[m
[32m+[m
[32m+[m[32m    async def set_preferences(self, values):[m
[32m+[m[32m        if set(values) - {"channel", "autostart"} or ("channel" in values and values["channel"] not in ("stable", "preview")) or ("autostart" in values and type(values["autostart"]) is not bool):[m
[32m+[m[32m            raise VPNError("Invalid preferences")[m
[32m+[m[32m        self.store.data["preferences"].update(values)[m
[32m+[m[32m        self.store.save()[m
[32m+[m[32m        return self.preferences()[m
[32m+[m
[32m+[m[32m    def prepare_update(self):[m
[32m+[m[32m        if self.state["state"] in ("CONNECTING", "RECONNECTING", "DISCONNECTING") or (self.task and not self.task.done()) or self.download_lock.locked() or self.lock.locked():[m
[32m+[m[32m            raise VPNError("Wait for the active VPN operation to finish")[m
[32m+[m[32m        self.maintenance = True[m
[32m+[m[32m        return True[m
[32m+[m
[32m+[m[32m    def resume_operations(self):[m
[32m+[m[32m        self.maintenance = False[m
[32m+[m[32m        return True[m
 [m
     async def ping_servers(self, subscription_id):[m
         subscription = self.find_subscription(subscription_id)[m
[36m@@ -442,10 +511,12 @@[m [mclass Service:[m
 [m
     async def connect(self, identifier):[m
         async with self.lock:[m
[31m-            if self.closing or self.core.alive or (self.task and not self.task.done()) or self.state["state"] == "DISCONNECTING":[m
[32m+[m[32m            if self.closing or self.maintenance or self.core.alive or (self.task and not self.task.done()) or self.state["state"] == "DISCONNECTING":[m
                 raise VPNError("A VPN operation is already running")[m
             n = copy.deepcopy(self.find_node(identifier))[m
             await self.select(identifier)[m
[32m+[m[32m            self.store.data["desired_connection"] = True[m
[32m+[m[32m            self.store.save()[m
             await self.set_state("CONNECTING", server=public_node(n), error=None, public_ip=None, verification=None, since=None)[m
             self.task = asyncio.create_task(self.run_connection(n))[m
         return self.status()[m
[36m@@ -497,6 +568,7 @@[m [mclass Service:[m
                 since=int(time.time()),[m
                 error=None,[m
             )[m
[32m+[m[32m            self.retry_delay = 2[m
         except asyncio.CancelledError:[m
             raise[m
         except Exception as e:[m
[36m@@ -506,9 +578,14 @@[m [mclass Service:[m
             except VPNError:[m
                 error = "VPN cleanup failed; retry Disconnect"[m
             await self.set_state("ERROR", error=error, public_ip=None)[m
[32m+[m[32m            self.retry_at = self.boottime() + self.retry_delay[m
[32m+[m[32m            self.retry_delay = min(self.retry_delay * 2, 60)[m
 [m
[31m-    async def disconnect(self):[m
[32m+[m[32m    async def disconnect(self, preserve_intent=False):[m
         async with self.lock:[m
[32m+[m[32m            if not preserve_intent:[m
[32m+[m[32m                self.store.data["desired_connection"] = False[m
[32m+[m[32m                self.store.save()[m
             await self.set_state("DISCONNECTING", error=None)[m
             if self.task and not self.task.done():[m
                 self.task.cancel()[m
[36m@@ -521,20 +598,53 @@[m [mclass Service:[m
             await self.set_state("DISCONNECTED", server=None, error=None, public_ip=None, verification=None, since=None)[m
             return self.status()[m
 [m
[32m+[m[32m    @staticmethod[m
[32m+[m[32m    def boottime():[m
[32m+[m[32m        return time.clock_gettime(time.CLOCK_BOOTTIME) if hasattr(time, "CLOCK_BOOTTIME") else time.monotonic()[m
[32m+[m
[32m+[m[32m    async def network_fingerprint(self):[m
[32m+[m[32m        if os.name != "posix":[m
[32m+[m[32m            return None[m
[32m+[m[32m        try:[m
[32m+[m[32m            process = await asyncio.create_subprocess_exec("ip", "-j", "route", "show", "table", "main", "default", stdout=asyncio.subprocess.PIPE, stderr=asyncio.subprocess.DEVNULL)[m
[32m+[m[32m            try:[m
[32m+[m[32m                data, _ = await asyncio.wait_for(process.communicate(), 3)[m
[32m+[m[32m            except BaseException:[m
[32m+[m[32m                process.kill()[m
[32m+[m[32m                await process.wait()[m
[32m+[m[32m                raise[m
[32m+[m[32m            routes = json.loads(data)[m
[32m+[m[32m            return hashlib.sha256(json.dumps(routes, sort_keys=True).encode()).hexdigest()[m
[32m+[m[32m        except Exception:[m
[32m+[m[32m            return None[m
[32m+[m
[32m+[m[32m    async def watch_tick(self, fingerprint, now):[m
[32m+[m[32m        resumed = now - self.last_tick > 15[m
[32m+[m[32m        changed = self.network_signature is not None and fingerprint is not None and fingerprint != self.network_signature[m
[32m+[m[32m        self.last_tick, self.network_signature = now, fingerprint[m
[32m+[m[32m        if self.maintenance or self.closing or not self.store.data["desired_connection"]:[m
[32m+[m[32m            return[m
[32m+[m[32m        if self.task and not self.task.done():[m
[32m+[m[32m            return[m
[32m+[m[32m        if self.state["state"] == "CONNECTED" and (not self.core.alive or changed or resumed or not await self.core.route_verified()):[m
[32m+[m[32m            await self.disconnect(preserve_intent=True)[m
[32m+[m[32m            if self.state["state"] == "ERROR":[m
[32m+[m[32m                return[m
[32m+[m[32m            await self.set_state("RECONNECTING", error=None)[m
[32m+[m[32m        if self.state["state"] in ("RECONNECTING", "ERROR", "DISCONNECTED") and now >= self.retry_at:[m
[32m+[m[32m            try:[m
[32m+[m[32m                await self.connect(self.store.data["selected"])[m
[32m+[m[32m            except VPNError:[m
[32m+[m[32m                self.retry_at = now + 10[m
[32m+[m
     async def watch(self):[m
         try:[m
             while True:[m
                 await asyncio.sleep(2)[m
[31m-                if self.state["state"] == "CONNECTED" and not self.core.alive:[m
[31m-                    async with self.lock:[m
[31m-                        if self.state["state"] != "CONNECTED":[m
[31m-                            continue[m
[31m-                        try:[m
[31m-                            await self.core.stop()[m
[31m-                            message = "VPN core stopped unexpectedly; reconnect to continue"[m
[31m-                        except VPNError:[m
[31m-                            message = "VPN cleanup failed; retry Disconnect"[m
[31m-                        await self.set_state("ERROR", error=message, public_ip=None)[m
[32m+[m[32m                try:[m
[32m+[m[32m                    await self.watch_tick(await self.network_fingerprint(), self.boottime())[m
[32m+[m[32m                except Exception:[m
[32m+[m[32m                    self.logger.info("Network recovery will retry")[m
         except asyncio.CancelledError:[m
             return[m
 [m
[36m@@ -569,16 +679,16 @@[m [mclass Service:[m
 [m
     def diagnostic(self):[m
         # Deliberately omit server/provider labels, host, URL, keys and raw core output.[m
[31m-        return json.dumps({"plugin": "0.1.4", "core": CORE_VERSION, "state": self.state["state"], "core_alive": self.core.alive, "cleanup_pending": (self.runtime / "network-owned.json").exists(), "subscription_count": len(self.store.data["subscriptions"]), "platform": "SteamOS/Linux required"}, indent=2)[m
[32m+[m[32m        return json.dumps({"plugin": VERSION, "daemon": VERSION, "protocol": PROTOCOL_VERSION, "core": CORE_VERSION, "state": self.state["state"], "core_alive": self.core.alive, "cleanup_pending": (self.runtime / "network-owned.json").exists(), "subscription_count": len(self.store.data["subscriptions"]), "platform": "SteamOS/Linux required"}, indent=2)[m
 [m
     def get_logs(self):[m
         return (self.logs / "plugin.log").read_text(encoding="utf-8")[-12000:][m
 [m
[31m-    async def close(self):[m
[32m+[m[32m    async def close(self, preserve_intent=False):[m
         self.closing = True[m
         if self.monitor:[m
             self.monitor.cancel()[m
             await asyncio.gather(self.monitor, return_exceptions=True)[m
[31m-        await self.disconnect()[m
[32m+[m[32m        await self.disconnect(preserve_intent=preserve_intent)[m
         self.log_handler.close()[m
         self.logger.removeHandler(self.log_handler)[m
[1mdiff --git a/vpn/storage.py b/vpn/storage.py[m
[1mindex 7012b86..b7a9d46 100644[m
[1m--- a/vpn/storage.py[m
[1m+++ b/vpn/storage.py[m
[36m@@ -2,9 +2,12 @@[m [mimport json[m
 import os[m
 import tempfile[m
 from pathlib import Path[m
[32m+[m[32mfrom .safe_fs import read_regular[m
 [m
 [m
 def private_dir(path: Path):[m
[32m+[m[32m    if any(p.is_symlink() for p in (path, *path.parents)):[m
[32m+[m[32m        raise RuntimeError("Unsafe storage directory")[m
     path.mkdir(parents=True, exist_ok=True, mode=0o700)[m
     if path.is_symlink():[m
         raise RuntimeError("Unsafe storage directory")[m
[36m@@ -13,6 +16,8 @@[m [mdef private_dir(path: Path):[m
 [m
 def atomic_json(path: Path, data):[m
     private_dir(path.parent)[m
[32m+[m[32m    if path.is_symlink():[m
[32m+[m[32m        raise RuntimeError("Unsafe storage file")[m
     fd, temporary = tempfile.mkstemp(prefix=".write-", dir=path.parent)[m
     try:[m
         with os.fdopen(fd, "w", encoding="utf-8") as stream:[m
[36m@@ -20,6 +25,12 @@[m [mdef atomic_json(path: Path, data):[m
             stream.flush()[m
             os.fsync(stream.fileno())[m
         os.replace(temporary, path)[m
[32m+[m[32m        if os.name == "posix":[m
[32m+[m[32m            directory = os.open(path.parent, os.O_RDONLY | os.O_DIRECTORY)[m
[32m+[m[32m            try:[m
[32m+[m[32m                os.fsync(directory)[m
[32m+[m[32m            finally:[m
[32m+[m[32m                os.close(directory)[m
     finally:[m
         if os.path.exists(temporary):[m
             os.unlink(temporary)[m
[36m@@ -33,8 +44,11 @@[m [mclass Store:[m
         if path.exists():[m
             if path.is_symlink():[m
                 raise RuntimeError("Unsafe storage file")[m
[31m-            self.data = json.loads(path.read_text(encoding="utf-8"))[m
[32m+[m[32m            self.data = json.loads(read_regular(path, 64 * 1024 * 1024))[m
             path.chmod(0o600)[m
[32m+[m[32m        self.data.setdefault("favorites", [])[m
[32m+[m[32m        self.data.setdefault("desired_connection", False)[m
[32m+[m[32m        self.data.setdefault("preferences", {"channel": "stable", "autostart": False})[m
 [m
     def save(self):[m
         atomic_json(self.path, self.data)[m
